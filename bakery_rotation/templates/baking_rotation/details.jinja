{% extends "baking_rotation/base.jinja" %}

{% block content %}

<div class="row">
  <h3>{{ item.item }}</h3>
  <div>{{ item.date }}</div>
  <div>{{ item.baker }}</div>
  <img src="{{ item.image.url }}" class="img-item">
</div>

{% endblock %}
